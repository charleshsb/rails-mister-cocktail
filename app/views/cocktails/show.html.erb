<div class="container">
  <h1><%= @cocktail.name %></h1>
  <%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill, class: "img-cocktail" %>
  <br>
  <br>
  <h3>Recipe :</h3>
  <ul class= "list-group">
    <% @cocktail.doses.each do |dose| %>
    <li class="list-group-item"><%= dose.ingredient.name %> : <%= dose.description %> - <%= link_to "🗑", cocktail_dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %></li>
    <% end %>
  </ul>
  <br>
  <p><%= link_to "Add a dose", new_cocktail_dose_path(@cocktail), class: "btn btn-light" %></p>
  <p><%= link_to "Back to list", cocktails_path, class: "btn btn-light" %></p>
</div>
